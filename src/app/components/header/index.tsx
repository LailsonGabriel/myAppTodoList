import { View, Text } from "react-native";
import React from "react";

import * as S from "./styles";

export default function Header() {
  return (
    <S.Container>
      <S.TextTitle>TodoList</S.TextTitle>
    </S.Container>
  );
}
